[project]
name = "flaskr"
version = "2.0.1"
description = "A basic blog app written using Flask tutorial"
dependencies = [
    "flask", "click"
]

[build-system]
requires = ["flit_core<4"]
build-backend = "flit_core.buildapi"
